---
id: {{feature_id}}
title: {{feature_title}}
owner_role: ta
status: draft
architecture_needed: false
architecture_reason: "既有架构内的同类增量（待 TA 基于 proposal 和代码影响面确认）"
template_version: story-v2-code-backed
---

# 用户故事 / Story设计说明书

> Owner: `ta`。Story 文档定义用户可感知价值、验收标准、依赖关系和 story 级设计约束；不要写 task ACL、执行命令或实现任务清单。
>
> `architecture_needed` 是流程路由依据。若评估为 `false`，必须写清跳过理由和 story 级设计约束；若评估为 `true`，必须创建或重构 `architecture.md`。

---

**修订记录**

| 日期 | 修订版本 | 修改描述 | 证据来源 |
| ---- | -------- | -------- | -------- |
|      | 0.1 | 初始创建/回填 | proposal、代码、测试 |

---

[TOC]

---

## 写作完成标准

- Story 必须能回连到 `proposal.md` 的 SR、场景或风险。
- AC 必须可验证，且在 `validation-report.md` 中复用相同 ID。
- 存量回填时必须写当前代码路径和测试路径；无法确认的行为写入“待确认问题”。
- 小规格可跳过架构，但必须在“架构影响评估”中证明它只是同类增量。

**Keywords 关键词**：

- 中文：
- 英文：

**Abstract 摘要**：

- 中文：
- 英文：

**术语和缩略语清单**：

| 术语 | 英文/代码名 | 中文解释 | 来源 |
| ---- | ----------- | -------- | ---- |
|      |             |          |      |

---

## Story 索引

| ID | 标题 | 优先级 | 状态 | 依赖 | 来源 SR |
|----|------|--------|------|------|---------|
| {{story_id}} | {{story_title}} | {{priority}} | ready | - | SR-001 |

# 1 架构影响评估

## 1.1 结论

- `architecture_needed`：false
- `architecture_reason`：既有架构内的同类增量（待 TA 基于证据确认）
- 若为 `true`，架构文档位置：[{{feature_title}} - 架构设计说明书](./architecture.md)

## 1.2 判断清单

| 判断项 | 是否命中 | 证据/说明 |
| ------ | -------- | --------- |
| 跨包、跨服务、跨编辑器或跨运行时 | 否 |  |
| 新增/改变公开 API、协议、DSL、数据模型 | 否 |  |
| 新增状态机、异步流程、权限/安全边界 | 否 |  |
| 涉及迁移、兼容、降级或回滚策略 | 否 |  |
| 仅同类 UI/交互/文案/样式小增量 | 是/否 |  |
| 无法判断影响半径 | 否 | 命中时必须改为 `true` |

## 1.3 跳过架构时的设计承载

> [!NOTE]
> `architecture_needed: false` 时，本节必须足够支撑 DEV 和 QA；不能只写“无需架构”。

- 复用的既有架构/组件：
- 不改变的接口/数据/状态：
- 需要 QA 重点验证的边界：

# 2 用户故事

## 2.1 {{story_id}} {{story_title}}

```yaml
id: {{story_id}}
title: {{story_title}}
priority: {{priority}}
status: ready
depends_on: []
source_requirements: [SR-001]
```

### 2.1.1 故事描述

- **作为**：{{actor}}
- **我想要**：{{capability}}
- **以便**：{{value}}
- **现状**：
- **目标状态**：
- **需求分析文档参考**：[{{feature_title}} - 需求分析说明书](./proposal.md)

### 2.1.2 范围

| 范围项 | 说明 | 证据来源 |
| ------ | ---- | -------- |
| 范围内 |  |  |
| 范围外 |  |  |

### 2.1.3 验收标准

| ID | Given 前置条件 | When 触发行为 | Then 可观察结果 | 验证提示 |
| -- | -------------- | ------------- | --------------- | -------- |
| `AC-1` | ... | ... | ... | 单元/组件/集成/人工验证 |
| `AC-2` | ... | ... | ... |  |

> [!NOTE] 编写要求
> - 每条 AC 只描述一个可验证结果。
> - AC 覆盖正常路径、关键异常路径、权限/边界条件和 proposal 中的 DFX 指标。
> - AC 写用户或系统可观察行为，不写“代码已实现”。

# 3 当前代码与测试证据（存量回填必填）

| 类型 | 路径/命令 | 覆盖 Story/AC | 结论 | 缺口 |
| ---- | --------- | ------------- | ---- | ---- |
| 代码 |  | `AC-1` |  |  |
| 测试 |  | `AC-1` |  |  |
| 运行/截图/日志 |  | `AC-1` |  |  |

# 4 Story 级设计约束

> [!NOTE]
> 这里写“为了满足 AC 必须遵守的设计约束”。如果需要完整架构设计，只写摘要并引用 `architecture.md`。

## 4.1 功能规格

| 规格ID | 规格描述 | 指标/限制 | 对应AC |
| ------ | -------- | --------- | ------ |
| SP-001 |          |           | `AC-1` |

## 4.2 接口与数据约束

| 对象 | 当前契约 | 本次变化 | 兼容策略 |
| ---- | -------- | -------- | -------- |
| API/Props/事件 |  | 不变/新增/调整 |  |
| 数据结构 |  | 不变/新增/调整 |  |

## 4.3 UI/交互约束

- UI 入口：
- 状态与反馈：
- 国际化/可访问性：
- 响应式/主题：

## 4.4 异常与边界

| 场景 | 系统行为 | 用户可见结果 | 覆盖AC |
| ---- | -------- | ------------ | ------ |
|      |          |              |        |

# 5 验证设计

| 测试ID | 类型 | 覆盖AC | 输入/前置 | 验证方式 | 预期结果 |
| ------ | ---- | ------ | --------- | -------- | -------- |
| VT-001 | 单元/组件/集成/视觉/人工 | `AC-1` |  |  |  |

## 5.1 自动化优先级

- 必须自动化：
- 可人工验证：
- 暂不验证及风险：

# 6 追溯矩阵

| 来源/SR | Story | AC | 代码路径 | 测试/验证 | QA 结果 |
| ------- | ----- | -- | -------- | --------- | ------- |
| SR-001 | {{story_id}} | `AC-1` |  |  | 待验证 |

# 7 风险与待确认问题

| ID | 类型 | 描述 | 影响 | 处理方式 |
| -- | ---- | ---- | ---- | -------- |
| R-001 | 风险 |  |  |  |
| OQ-001 | 待确认 |  |  |  |

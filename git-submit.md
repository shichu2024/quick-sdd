# Git 提交规范

## 一、规范概述

本规范采用「**类型标识 + 特性编号 + 中文描述**」的结构化提交格式，确保提交历史清晰可追溯，便于代码审查、版本回滚与变更日志生成。提交消息分为**标题**与**正文**两部分，正文必须包含代码变更的简要描述；若提交上下文包含大模型思考过程，需完整附在提交内容中。

---

## 二、提交消息结构

```
<标题>

<变更简要描述>

<大模型思考过程（可选）>
```

### 格式规则
1. **标题**：单行，不超过 72 字符，严格遵循各类型固定前缀格式。
2. **空行**：标题与正文之间必须保留一行空行。
3. **变更描述**：使用中文，清晰说明本次提交做了什么、为什么这么做、核心改动点，避免只写"修复bug"这类模糊表述。
4. **大模型思考过程**：若提交代码由大模型辅助生成，需将完整思考链路基带在正文末尾，以 `【AI 思考过程】` 作为标识开头。

---

## 三、各类型提交格式详解

### 1. 新增特性（FEAT）
**格式**：`FEAT-xxx 特性描述信息`
- `xxx` 为特性编号（数字或业务标识），用于唯一标识该功能模块。
- 描述信息简要说明新增了什么功能。

**示例标题**：
```
FEAT-001 用户登录模块新增短信验证码登录方式
```

---

### 2. 特性功能迭代（Iterate）
**格式**：`Iterate（FEAT-xxx） 特性迭代描述信息`
- 基于已有特性进行功能优化、逻辑调整、体验升级等迭代开发。
- 括号内必须关联对应的特性编号。

**示例标题**：
```
Iterate（FEAT-001）优化短信验证码发送频率限制策略
```

---

### 3. 问题修复（Fix）
**格式**：`Fix（FEAT-xxx 可选）修复信息`
- 修复线上或测试环境的 Bug、异常、逻辑错误。
- 若修复关联特定特性，括号内填写特性编号；通用修复可省略特性编号，写作 `Fix 修复信息`。

**示例标题**：
```
Fix（FEAT-001）修复短信验证码倒计时结束后按钮未恢复可点击状态的问题
Fix 修复全局工具函数日期格式化在闰年二月的计算错误
```

---

### 4. 文档更新（Doc）
**格式**：`Doc 文档更新信息`
- 涵盖 README、接口文档、设计文档、注释、使用说明等所有文档类变更。
- 不涉及业务代码修改。

**示例标题**：
```
Doc 补充用户登录模块接口参数说明与错误码对照表
```

---

### 5. 其他场景
| 类型前缀 | 适用场景 | 格式示例 |
|---------|---------|---------|
| `Refactor` | 代码重构，不改变功能逻辑 | `Refactor（FEAT-001）重写登录校验逻辑，提升代码可读性` |
| `Style` | 代码格式调整、空格、缩进、命名规范等 | `Style 统一全局变量命名规范为小驼峰` |
| `Perf` | 性能优化 | `Perf（FEAT-001）优化用户信息查询SQL，添加索引提升查询速度` |
| `Test` | 测试用例新增或修改 | `Test（FEAT-001）补充短信验证码登录接口单元测试` |
| `Chore` | 构建工具、依赖配置、CI/CD 等工程化变更 | `Chore 升级项目依赖包版本至最新稳定版` |
| `Revert` | 回滚之前的提交 | `Revert 回滚 FEAT-001 短信验证码登录相关提交` |

---

## 四、大模型思考过程嵌入规范

当本次提交的代码由大语言模型辅助生成或设计时，**必须在提交正文末尾附上完整的思考过程**，便于后续维护者理解设计思路与决策依据。

### 嵌入格式
```
【AI 思考过程】
----------------
<此处粘贴完整的大模型思考内容，包括需求分析、方案选型、逻辑推导、边界处理等>
----------------
```

### 要求
1. 保留原始思考链路，不做精简删减，确保可追溯完整的决策过程。
2. 若思考过程包含多轮对话，按顺序完整保留。
3. 思考过程放在正文最末尾，与变更描述之间空一行。

---

## 五、完整提交示例

### 示例 1：新增特性 + AI 思考过程
```
FEAT-001 用户登录模块新增短信验证码登录方式

新增短信验证码登录接口，集成第三方短信服务，支持 60 秒倒计时
与防刷机制。包含手机号格式校验、验证码有效期校验、登录态
生成三个核心流程。数据库新增 sms_code 表存储验证码记录。

【AI 思考过程】
----------------
用户需要实现短信验证码登录功能，首先拆解需求：
1. 前端需要发送验证码接口和登录接口
2. 后端需要生成随机6位数字验证码，存入数据库并设置5分钟有效期
3. 调用第三方短信网关发送，考虑失败重试机制
4. 防刷策略：同一手机号1分钟内只能发1次，1小时内最多5次
5. 登录校验通过后生成JWT token返回
...
----------------
```

### 示例 2：问题修复
```
Fix（FEAT-001）修复短信验证码倒计时结束后按钮未恢复可点击状态的问题

修复前端登录页倒计时逻辑中，定时器清除时机错误导致的按钮状态
未重置问题。将 clearInterval 移至倒计时为 0 的判断分支内，
并补充页面卸载时的定时器销毁逻辑，避免内存泄漏。
```

### 示例 3：文档更新
```
Doc 补充用户登录模块接口文档

新增短信验证码发送接口与验证码登录接口的文档说明，包含请求
参数、响应字段、错误码对照表以及调用示例。补充限流规则说明。
```

---

## 六、最佳实践

1. **原子化提交**：一个提交只做一件事，避免多个功能混在一次提交中，便于回滚与排查。
2. **描述清晰**：避免"修复bug"、"优化代码"这类模糊表述，说明白修复了什么、优化了什么。
3. **控制粒度**：单次提交代码量不宜过大，建议不超过 400 行变更，便于 Code Review。
4. **关联特性编号**：所有业务相关的提交尽量关联 FEAT 编号，方便按特性维度汇总变更记录。
5. **AI 思考必带**：凡是借助大模型生成的代码、方案，提交时必须附上思考过程，形成可追溯的知识沉淀。
